#include <bits/stdc++.h>
using namespace std;

int main() {
	long long int n;
	unordered_map<long long int,long long int>m;
	cin>>n;
	for(long long int i=0; i<n; i++){
		long long int mkh;
		cin>>mkh;
		m[mkh] = 1;
	}
	cout<<m.size();
	return 0;
}
